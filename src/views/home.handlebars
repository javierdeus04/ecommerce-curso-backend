<h1>Lista de productos con Handlebars</h1>
<div class="container">
    {{#if products}}
    {{#each products}}
    <article>
        <h2>{{title}}</h2>
        <p>{{category}}</p>
        <p>{{description}}</p>
        <p>${{price}}</p>
    </article>
    {{/each}}
    {{else}}
    <p>No hay productos disponibles.</p>
    {{/if}}
</div>